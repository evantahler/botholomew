---
alwaysApply: false
description: When writing backend code
---

Whenever possible, use the `TypedError` class to throw errors.

After any modification of the models, we need to migrate via `bun run botholomew.ts migrate`
